import Converter from "../utils/converter";

export default function Modifier(props) {
    const modfier = props.modfier.modfier;
    const type = props.modfier.type;
    // const roll = props.modfier.
    const converter = new Converter();
}